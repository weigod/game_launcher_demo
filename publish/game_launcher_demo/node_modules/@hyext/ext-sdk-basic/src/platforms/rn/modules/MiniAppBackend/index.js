import modeManager from '../../common/modeManager';

export default {
  async commonQuery(param) {
    const MiniAppBackend = modeManager.getModule('MiniAppBackend');
    const responseJson = await MiniAppBackend.commonQuery(param);
    return JSON.parse(responseJson);
  }
}