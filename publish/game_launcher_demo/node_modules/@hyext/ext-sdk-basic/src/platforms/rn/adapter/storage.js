import modeManager from '../common/modeManager';

let miniAppStorage = null;
export default {
  getStorage() {
    if (!miniAppStorage) {
      miniAppStorage = modeManager.getModule('MiniAppStorage');
    }
    return miniAppStorage || {};
  }
};