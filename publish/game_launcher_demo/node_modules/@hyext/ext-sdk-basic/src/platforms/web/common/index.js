import messager from './messager';
import topicListener from './topicListener';
import nativeEventer from './nativeEventer';
import h5Base from '../../../utils/h5Base';

export default {
  messager,
  topicListener,
  nativeEventer,
  utils: h5Base
}