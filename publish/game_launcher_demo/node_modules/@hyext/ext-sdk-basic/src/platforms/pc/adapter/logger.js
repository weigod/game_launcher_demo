import messager from '../common/messager';

export default {
  /**
   * platform 标记，必填
   */
  TAG: 'EXT_PC',

  /**
   * 上报日志
   * @param {string} type 
   * @param {any} data 
   */
  postLog(type, data) {
    return messager.emit('log', data).catch(err => { });
  }
};
