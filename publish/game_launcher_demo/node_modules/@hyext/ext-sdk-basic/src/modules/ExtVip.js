import common from '../common';

const MODULE_NAME = 'MiniAppVip';

export default {
  /**
   * @namespace vip
   */
  vip: {
    /**
     * 获取小程序授权token
     * @return {Promise<object>} 返回小程序授权token
     */
    getJWT() {
      return common.core.callEvent(MODULE_NAME, 'getJWT');
    },

  }
}
