const _global = (typeof window !== 'undefined' && window) || (typeof global !== 'undefined' && global) || {};

if (!_global.$_sdk_) {
  _global.$_sdk_ = {};
  _global.$_sdk_.platform = {
    adapter: {}, // 适配器占位
    modules: {}, // 模块占位
  };
}

export default {
  /**
   * global 映射
   */
  win: _global,

  /**
   * 当前平台实例
   */
  platform: _global.$_sdk_.platform
}
