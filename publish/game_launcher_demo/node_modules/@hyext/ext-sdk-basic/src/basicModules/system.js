import system from '../common/system';

export default {
  /**
   * @typedef SysConfig
   * @property {string} version SDK版本号
   * @property {string} platform 当前平台信息
   */
  /**
   * 获取当前配置信息
   * @returns {SysConfig} SDK配置信息
   */
  getConfig() {
    return system.getConfig();
  }
}
